# Financial Advisor Agent Environment Configuration
# Copy this file to .env and update the values for your deployment

# =============================================================================
# Google Cloud Configuration
# =============================================================================
GOOGLE_CLOUD_PROJECT=agent-space-demo-475212
GOOGLE_CLOUD_LOCATION=us-central1
GOOGLE_CLOUD_STORAGE_BUCKET=fin-advisor-001
GOOGLE_GENAI_USE_VERTEXAI=true

# =============================================================================
# Deployed Agent Configuration
# =============================================================================
AGENT_RESOURCE_ID=7500098666548428800
DEFAULT_USER_ID=financial_advisor_user

# =============================================================================
# MCP Server Integration
# =============================================================================
# Set to true to use HTTP MCP server instead of direct DB connection
USE_MCP_HTTP_SERVER=true

# MCP Server URL (set after deploying MCP server to Cloud Run)
MCP_SERVER_URL=https://finadvisor-mcp-server-xxxxxxxxx-uc.a.run.app

# =============================================================================
# Database Configuration (for direct connection fallback)
# =============================================================================
# Database connection details (used when USE_MCP_HTTP_SERVER=false)
DB_USER=finadvisor_user
DB_PASSWORD=FinAdvisorUser2024!
DB_NAME=FinAdvisor
DB_HOST=34.29.136.71
DB_PORT=5432

# CloudSQL instance connection name
CLOUDSQL_CONNECTION_NAME=agent-space-demo-475212:us-central1:finadvisor-db

# =============================================================================
# Development Configuration
# =============================================================================
# Set to true for development mode (enables debug logging)
DEBUG=false

# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# =============================================================================
# Agent-Specific Configuration
# =============================================================================
# Default session and user IDs for agent operations
DEFAULT_SESSION_ID=default_session
DEFAULT_USER_ID=financial_advisor_user

# Agent execution settings
AGENT_TIMEOUT=300
MAX_RETRIES=3

